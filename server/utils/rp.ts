export const rpName = !import.meta.dev ? 'mail.sh0rt.kr' : 'localhost'
export const rpID =!import.meta.dev ? 'mail.sh0rt.kr' : 'localhost'
export const rpOrigin = !import.meta.dev ? `https://${rpID}` : `http://${rpID}:3000`